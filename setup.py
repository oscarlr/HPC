from setuptools import setup, find_packages

setup(
    name='HPC',
    url='https://github.com/oscarlr/HPC',
    description='Small class to submit jobs into Minerva using Harms submitjob',
    packages=['HPC'],
    include_package_data=True,
    platforms='any'
)
